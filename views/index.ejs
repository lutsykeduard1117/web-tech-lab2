<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DIY Blog</title>
    <!-- Підключення Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .post {
            margin-bottom: 20px;
            padding: 15px;
            border: 1px solid #ddd;
            border-radius: 5px;
            background-color: #f9f9f9;
        }
        .post-title {
            font-size: 1.5em;
            font-weight: bold;
        }
        .post-description {
            font-size: 1em;
            color: #555;
        }
    </style>
</head>
<body>
<div class="container mt-4">
    <h1 class="mb-4 text-center">Welcome to the DIY Blog</h1>
    <nav class="mb-4">
        <a href="/privacy" class="btn btn-outline-secondary">Privacy Policy</a>
        <a href="/about" class="btn btn-outline-secondary">About</a>
        <a href="/add" class="btn btn-primary">Add New Item</a>
    </nav>
    <div class="row">
        <% items.forEach(item => { %>
            <div class="col-md-6">
                <div class="post">
                    <div class="post-title"><%= item.name %></div>
                    <div class="post-description"><%= item.description %></div>
                    <a href="/item/<%= item.id %>" class="btn btn-link">Read More</a>
                </div>
            </div>
        <% }) %>
    </div>
</div>
<!-- Підключення Bootstrap JavaScript -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
